# Public DNS name whose A/AAAA record points to the deployment host.
GFS_WEB_HOST=studio.example.com

# Path to a file containing an independent product login secret with at least
# 32 random characters. Never reuse a provider credential.
GFS_WEB_ACCESS_TOKEN_FILE=./secrets/gfs_web_access_token
